schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-multihop-002"
description = "Andrej Karpathy gives a reason for doubting current agents. Does a later episode address that same limitation, and does it agree?"
authors = []
keywords = ["dwarkesh", "retrieval", "bounded_multi_hop", "dev"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "multihop-002"
split = "dev"
question_class = "bounded_multi_hop"
topics = ["agents", "llm_limits"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
