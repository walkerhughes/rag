schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-synth-001"
description = "What reasons do guests give for why current models cannot learn on the job?"
authors = []
keywords = ["dwarkesh", "retrieval", "cross_episode_synthesis", "dev"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "synth-001"
split = "dev"
question_class = "cross_episode_synthesis"
topics = ["continual_learning"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
