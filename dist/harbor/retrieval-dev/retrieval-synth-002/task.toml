schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-synth-002"
description = "What bottlenecks to scaling compute are described across episodes?"
authors = []
keywords = ["dwarkesh", "retrieval", "cross_episode_synthesis", "dev"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "synth-002"
split = "dev"
question_class = "cross_episode_synthesis"
topics = ["compute_scaling"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
