schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-multihop-001"
description = "Richard Sutton calls a particular approach a dead end. Which other guests discuss that same approach, and do they share his conclusion?"
authors = []
keywords = ["dwarkesh", "retrieval", "bounded_multi_hop", "dev"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "multihop-001"
split = "dev"
question_class = "bounded_multi_hop"
topics = ["llm_limits"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
