schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-synth-004"
description = "Which episodes discuss how geography constrained a great power's strategy, and what constraint is described?"
authors = []
keywords = ["dwarkesh", "retrieval", "cross_episode_synthesis", "heldout"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "synth-004"
split = "heldout"
question_class = "cross_episode_synthesis"
topics = ["grand_strategy"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
