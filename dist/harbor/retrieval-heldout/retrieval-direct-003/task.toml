schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-direct-003"
description = "In a continual learning setup, where does Richard Sutton say the information goes, in contrast to a language model's context window?"
authors = []
keywords = ["dwarkesh", "retrieval", "direct_retrieval", "heldout"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "direct-003"
split = "heldout"
question_class = "direct_retrieval"
topics = ["continual_learning"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
