schema_version = "1.3"

[task]
name = "walkerhughes/retrieval-synth-003"
description = "What do guests say about whether scaling alone is enough to reach AGI?"
authors = []
keywords = ["dwarkesh", "retrieval", "cross_episode_synthesis", "heldout"]

# Where this task came from, so a hub result resolves back to the example.
[metadata]
category = "retrieval"
example_id = "synth-003"
split = "heldout"
question_class = "cross_episode_synthesis"
topics = ["scaling_laws", "agi_timelines"]

[agent]
timeout_sec = 600.0

[verifier]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
